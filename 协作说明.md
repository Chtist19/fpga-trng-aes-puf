# GitHub 协作说明

## 1. 分支约定

建议三个人分别建自己的功能分支：

```text
main                 稳定版本
feature/trng         TRNG 修改
feature/aes          AES 修改
feature/puf          PUF 修改
docs/report          汇报和文档
```

平时不要直接改 `main`，先在自己的分支提交，再通过 Pull Request 合并。

## 2. 提交前检查

每次提交前至少确认：

```powershell
git status
```

不要提交这些内容：

```text
*.bin
*.zip
project_*/project_*.runs/
project_*/project_*.cache/
project_*/project_*.gen/
power_perf/
```

如果误加入了大文件，可以先取消暂存：

```powershell
git restore --staged 文件名
```

## 3. 推荐提交信息

提交信息用简短中文即可，例如：

```text
新增 TRNG 与 AES 密钥接口说明
修正 UART 帧包尾判断
更新 PUF challenge 对接说明
```

## 4. 对接口修改规则

涉及接口的改动必须同步修改文档：

```text
TRNG_module_package/docs/接口说明.md
TRNG_module_package/docs/AES对接说明.md
TRNG_module_package/docs/PUF对接说明.md
```

例如改了 `key_data_o` 字节顺序，就必须在接口说明里同步更新。

## 5. GitHub 远程仓库创建方式

如果本机没有 GitHub CLI，可以先在网页创建仓库：

```text
https://github.com/new
```

建议仓库名：

```text
fpga-trng-aes-puf
```

创建时不要勾选自动生成 README，因为本地已经有 README。

网页创建后，在本地执行：

```powershell
git remote add origin https://github.com/你的用户名/fpga-trng-aes-puf.git
git branch -M main
git push -u origin main
```

## 6. 队友拉取方式

队友第一次下载：

```powershell
git clone https://github.com/你的用户名/fpga-trng-aes-puf.git
```

后续更新：

```powershell
git pull
```
